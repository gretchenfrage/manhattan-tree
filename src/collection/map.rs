
use tree::*;

/*
pub struct MTreeMap<T> {
    tree: MTree<T>
}
impl<T> MTreeMap<T> {
    pub fn new() -> Self {
        MTreeMap {
            tree: MTree::new()
        }
    }

    pub fn get(&self, key: [u64; 3]) ->
}
*/